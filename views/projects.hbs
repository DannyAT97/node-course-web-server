<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Porfolio Page</title>
    </head>    
    <body>
        <h1>{{pageTitle}}</h1>
        {{> header}}
        <p>This is {{name}}'s new porfolio. {{info}}</p> 
        {{> footer}}
    </body>
</html>